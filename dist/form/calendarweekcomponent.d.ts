type UXMode = 'continuous' | 'loop' | 'fixed';
type CalendarweekElements = 'component' | 'week' | 'year';
type Action = (week: number, year: number, date: Date) => any;
export declare class CalendarweekComponent {
    private container;
    private weekInput;
    private yearInput;
    private week;
    private year;
    private minDate;
    private maxDate;
    private minDateYear;
    private maxDateYear;
    private minDateWeek;
    private maxDateWeek;
    private currentMinWeek;
    private currentMaxWeek;
    private mode;
    private onChangeActions;
    constructor(container: HTMLElement, mode?: UXMode);
    static select: import("../index.js").AttributeSelector<CalendarweekElements>;
    setDate(date: Date, silent?: boolean): void;
    setMode(mode: UXMode): void;
    setMinMaxDates(newMinDate: Date | null, newMaxDate: Date | null): void;
    addOnChange(callback: Action): void;
    removeOnChange(callback: Action): void;
    getCurrentDate(): Date;
    private parseWeekAndYear;
    private onChange;
    private updateClient;
    private updateClientWeekMinMax;
    private updateClientValues;
    private onYearChange;
    private onWeekChange;
    private updateWeekMinMax;
    private onWeekKeydown;
    private keepYearInBounds;
    private keepWeekInBounds;
    private onYearKeydown;
}
export {};
